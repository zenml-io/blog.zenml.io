# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
# Site settings
title: ZenML | Blog
owner: ZenML GmbH
email: hello@zenml.io
description: 
logo: assets/logo_sq.png
nav_logo: assets/logo_sq.png
phone: ""
author: ZenML GmbH
future: true 
repository: zenml-io/blog.zenml.io
url: https://blog.zenml.io
baseurl: ""

links:
  documentation: "https://docs.zenml.io"
  github: "https://github.com/zenml-io/zenml"
  slack-invite: "https://zenml.io/slack-invite/"
  email: "mailto:support@zenml.io"
  zenml: "https://zenml.io"
  newsletter: "https://zenml.io/newsletter"
  roadmap: "https://zenml.io/roadmap"
  careers: "https://zenml.io/careers"

# Social networks usernames (many more available: google-plus, flickr, dribbble, pinterest, instagram, tumblr, linkedin, etc.)
social:
  linkedin: https://www.linkedin.com/company/zenml
  links:
    - https://twitter.com/zenml_io
    - https://github.com/zenml-io/zenml
    - https://www.crunchbase.com/organization/zenml

address:
  street: Clemensstraße 99
  city: 80796 München

# Build settings
markdown: kramdown
permalink: /:title/
highlighter: rouge

mini_magick:
    thumbnail: # Preset name
        source: img # source directory - change this to whatever you want
        destination: img/thumbs # generated destination directory
        resize: "100x70^" # standard imagemagick options - you can chain multiple commands
        gravity: "center"
        extent: "100x70"



plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate-v2
  - jekyll-redirect-from
  # - jekyll-compress-images

############################################################
# Site configuration for the Jekyll 3 Pagination Gem
# The values here represent the defaults if nothing is set
pagination:
  # Site-wide kill switch, disabled here it doesn't run at all 
  enabled: true

  # Set to 'true' to enable pagination debugging. This can be enabled in the site config or only for individual pagination pages
  debug: false

  # The default document collection to paginate if nothing is specified ('posts' is default)
  collection: 'posts'

  # How many objects per paginated page, used to be `paginate` (default: 0, means all)
  per_page: 10

  # The permalink structure for the paginated pages (this can be any level deep)
  permalink: '/page/:num/' # Pages are index.html inside this folder (default)
  #permalink: '/page/:num.html' # Pages are simple html files 
  #permalink: '/page/:num' # Pages are html files, linked jekyll extensionless permalink style.

  # Optional the title format for the paginated pages (supports :title for original page title, :num for pagination page number, :max for total number of pages)
  title: ':title - page :num'

  # Limit how many pagenated pages to create (default: 0, means all)
  limit: 0
  
  # Optional, defines the field that the posts should be sorted on (omit to default to 'date')
  sort_field: 'date'

  # Optional, sorts the posts in reverse order (omit to default decending or sort_reverse: true)
  sort_reverse: true

  # Optional, the default category to use, omit or just leave this as 'posts' to get a backwards-compatible behavior (all posts)
  category: 'posts'

  # Optional, the default tag to use, omit to disable
  tag: ''

  # Optional, the default locale to use, omit to disable (depends on a field 'locale' to be specified in the posts, 
  # in reality this can be any value, suggested are the Microsoft locale-codes (e.g. en_US, en_GB) or simply the ISO-639 language code )
  locale: '' 

 # Optional,omit or set both before and after to zero to disable. 
 # Controls how the pagination trail for the paginated pages look like. 
  trail: 
    before: 2
    after: 2

  # Optional, the default file extension for generated pages (e.g html, json, xml).
  # Internally this is set to html by default
  extension: html

  # Optional, the default name of the index file for generated pages (e.g. 'index.html')
  # Without file extension
  indexpage: 'index'


feed:
  posts_limit: 20
  collections:
    - blog
    # blog:
    #   path: "/feed.xml"

# tag_page_layout: tag_page
# tag_page_dir: tag
# tag_permalink_style: pretty
# tag_page_data:
#   sitemap: false

defaults:
  - scope:
      path: "signatures/**"
    values:
      sitemap: false
  - scope:
      path: "clients/**"
    values:
      sitemap: false
  - scope:
      path: "jobs/**"
    values:
      sitemap: false
  - scope:
      path: "services/**"
    values:
      sitemap: false
  - scope:
      path: "press/**"
    values:
      sitemap: false
  - scope:
      path: "jobs/**"
    values:
      sitemap: false

exclude: [README.md, website-config.json, Dockerfile, Gemfile, Gemfile.lock, scripts]
